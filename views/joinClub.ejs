<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Join The Club</title>
  </head>
  <body>
    <h1>Join The Club</h1>
    <form action="/join-club" method="post">
      <div>
        <label for="inviteCode">Invite code</label>
        <input type="password" name="inviteCode" id="inviteCode" />
      </div>
      <button type="submit">Submit</button>
    </form>
    <% if (locals.errors) { %> <% errors.map( err => { %>
    <p style="color: red"><%= err.msg %></p>
    <% }) %> <% } %>
    <button type="button"><a href="/">Back home</a></button>
  </body>
</html>
